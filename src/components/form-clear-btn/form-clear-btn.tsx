import { Component, Element, Event, Prop, State, h, EventEmitter } from '@stencil/core';

@Component({
  tag: 'form-clear-btn',
  styleUrl: 'form-clear-btn.css',
  shadow: true,
})
export class AddRemoveBtn {


  render() {
    return ()
  }
}
